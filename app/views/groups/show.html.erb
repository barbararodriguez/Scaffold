<div class="container">
    <div class="page-header">
      <h1>Grupo</h1>
    </div>

    <div class="container">
        <h2>Nombre:  <small><%= @group.name %></small></h2>
    
    <%= link_to 'Editar', edit_group_path(@group), :class => "btn btn-warning btn-xs" %>
    </div>

    <div class="page-header">
      <h3>Alumnos</h3>
    </div>
    <div class="row">
        <div class="col-md-6 col-xs-12 col-lg-6">
    <table class="table table-hover">
        <thead>
            
            <tr>
                <th>Nombre</th>
                <th>Edad</th>
                <th></th>
            </tr>
        </thead>
    <% @group.students.each do |student| %>
        
        <tbody>
            <tr>
                <td><%= student.name %></td>
                <td><%= student.age %></td>
                <td><%= link_to 'Eliminar alumno', [student.group, student],
                       method: :delete,
                       data: { confirm: '¿Estas seguro?' }, :class => "btn btn-danger btn-xs" %></td>
            </tr>
        
        </tbody>
    <% end %>
            
    </table> 
     

    <h3>Agregar un alumno:</h3>
    <%= render "students/form" %>

    <%= link_to 'Regresar', groups_path, :class => "btn btn-primary"  %>
            </div>
    </div>
    <br>
    <br>
    <br>
</div>
